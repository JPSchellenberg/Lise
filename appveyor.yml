version: 0.4.{build}

artifacts:
  - path: dist
    name: dist
    type: zip

platform:
  - x64

cache:
  - node_modules
  - app\node_modules
  - '%APPDATA%\npm-cache'
  - '%USERPROFILE%\.electron'

init:
  - git config --global core.autocrlf input

install:
  - ps: Install-Product node 6 x64
  - git reset --hard HEAD
  - npm install npm -g
  - npm install electron-builder@next # force install next version to test electron-builder
  - npm install
  - npm prune

build_script:
  - node --version
  - npm --version
  - copy client\index.html app\index.html 
  - copy electron\* app\ 
  - xcopy /E /I client\assets app\client\
  - node_modules\.bin\webpack -p --config webpack.production.js --progress --colors
  - node_modules\.bin\build -w

test: off